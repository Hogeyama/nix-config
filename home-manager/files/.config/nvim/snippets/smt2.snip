
snippet     HORN
options     head
	(set-logic HORN)
	(declare-fun S (Int) Bool)
	
	(assert (forall ((n Int))
	  (=> (S n) false)))
	${0}
	
	(check-sat)
	(get-model)

snippet     assert
options     head
	(assert (forall ((${1}))
	  (=> (and ${2})
	      ${3})))

